# Images

The first migration preview hot-links the existing images from `npm3d.fr` so the repository can be published immediately.

Before the old server is shut down, copy those images into this directory and replace their URLs in `_data/datasets.yml` and the six dataset page front matters. See `MIGRATION.md`.
